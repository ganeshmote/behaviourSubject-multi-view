import { TestappDirective } from './testapp.directive';

describe('TestappDirective', () => {
  it('should create an instance', () => {
    const directive = new TestappDirective();
    expect(directive).toBeTruthy();
  });
});
